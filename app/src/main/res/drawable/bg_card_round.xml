<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Rounded corners -->
    <corners android:radius="12dp"/>

    <!-- Border only -->
    <stroke
        android:width="1dp"
        android:color="@color/boarderOfTextEditor"/>

    <!-- Optional: transparent fill so only border shows -->
    <solid android:color="@color/fragmentBackground"/>
</shape>